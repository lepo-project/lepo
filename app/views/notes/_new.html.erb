<div id="note-resource" class="scroll-pane">
  <div class="row" id="new-note">
    <div class="col-md-12">
      <div class="head">
        <div class="header-explanation">
          <%= t('.required') %>：<span class="required">*</span>
        </div>
        <h1><%= t('activerecord.others.note.category.' + @note.category) %></h1>
        <%= render partial: 'notes/form', locals: {submit_hash: {action: 'ajax_create'}, cancel_hash: {action: 'ajax_index', nav_section: 'home'}} %>
      </div>
    </div>
  </div>
</div>
